autoware:
  root: "/autoware"
  ros_setup_script: "/opt/ros/humble/setup.bash"

  launch:
    package: "autoware_launch"
    file: "sbsvf.launch.xml"
    headless: false
    log_path: "./autoware_launch_2.log"
    # extra_args:
    # - "launch_rviz:=false"
    # - "launch_system_monitor:=false"
    # - "launch_perception:=true"

  data:
    map_base_path: "/autoware_map"
    default_map_subdir: "sample-map-planning"
    data_path: "/autoware_data"

  vehicle:
    model: "sample_vehicle"
    sensor_model: "sample_sensor_kit"

  runtime:
    timeout_sec: 100.0
    control_timeout_sec: 0.01
    allow_goal_modification: false
    wait_control: false # wait for current ROS2 control message at each sim step (it will make simulation slower), if false, use last received control command
